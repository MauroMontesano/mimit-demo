import { ClickFromKeyboardWebkitDirective } from './click-from-keyboard-webkit.directive';

describe('ClickFromKeyboardWebkitDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickFromKeyboardWebkitDirective();
    expect(directive).toBeTruthy();
  });
});
