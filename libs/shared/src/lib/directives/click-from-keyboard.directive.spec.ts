import { ClickFromKeyboardDirective } from './click-from-keyboard.directive';

describe('ClickFromKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickFromKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
